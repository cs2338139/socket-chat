import { State } from "@interfaces/State";
import { User } from "@interfaces/User";
import { Message } from "@interfaces/Message";
import { SocketID, Room, MessageID, MessageContent, UserName } from "./Types.type";

export type { State, User, Message, SocketID, Room, MessageID, MessageContent, UserName };
